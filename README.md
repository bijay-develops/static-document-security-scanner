## A static document security scanner that:

- Walks directories recursively
- Analyzes files using pluggable analyzers
- Computes SHA256
- Detects macro-enabled Word docs
- Detects suspicious PDF indicators
- Produces structured JSON
- Uses a worker pool for concurrency

It’s a foundation.